# Project Context Report

## Project Overview
- **Project Name:** Clinic Management System (`clinic-app`)
- **Type:** Full-stack Web Application
- **Path:** `c:\Users\sahup\OneDrive\Documents\Clinic Management System`

## Core Technologies
- **Framework:** Next.js (v16.1.7) using the App Router (`src/app` directory).
- **Library:** React (v19.2.3) & React DOM (v19.2.3).
- **Language:** TypeScript (`@types/node`, `@types/react`, `@types/react-dom`, `typescript` v5).
- **Styling:** Tailwind CSS (v4) configured with PostCSS.
- **Authentication:** NextAuth.js (`next-auth` v5.0.0-beta.30).

## Application Structure & Routing
The app is structured to serve three distinct user roles, reflecting a clinic management scenario:
1. **/admin:** Administration panel and routes.
2. **/staff:** Clinic staff interface.
3. **/patient:** Patient portal.

## Authentication System
- NextAuth configurations reside in `src/app/api/auth/`.
- UI Authentication components include `GoogleButton.tsx` (suggesting OAuth integration via Google) and `SignupForm.tsx` (suggesting email/password registration capabilities).

## Development Setup
- Uses `eslint` for linting.
- Standard Next.js scripts are available (`npm run dev`, `npm run build`, `npm run start`).

## Active Files & Recent Development Focus
- **Current Active Files:**
  - `Context Report.txt`
  - `src/app/staff/page.tsx` (Staff dashboard UI updates)
  - `src/lib/mockDb.ts` (Mock database for NextAuth)
  - `auth.ts` (NextAuth configuration using mock DB)
  - `c:\tmp\test-supabase.js` (Supabase connection and query test script)
  - `c:\tmp\list-roles.js` (Testing Supabase roles)
- **Recent Focus:** 
  - Setting up Authentication (NextAuth) and testing database integration using **Supabase**.
  - Replacing the mock database with actual Supabase tables.
  - Updating the Staff Dashboard UI (`src/app/staff/page.tsx`) with new icons and medical consultation data.
  - Provisioned a demo staff account (`staff@demo.com` / `staffPassword123`) directly in the Supabase database.
